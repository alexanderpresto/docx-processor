"""
Version information for the docx-processor package.

This module is isolated to prevent circular imports. It should only contain
version-related constants and simple functions that don't depend on other
modules in the package.

Usage:
    from docx_processor.version import __version__
"""

# Version information
__version__ = '0.1.0'
